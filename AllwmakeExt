#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory
set -x

cd incompressible
	./Allwmake
cd ..

cd scalarFluxFoam/scalarFluxFoam-ext
	wmake
cd ..


# ----------------------------------------------------------------- end-of-file
